# Base URL del backend Django, sin slash final.
# Copia este archivo a ".env" (para local) y ajusta el valor según el entorno.

# Desarrollo local (Django corriendo con `manage.py runserver`):
VITE_API_URL=http://127.0.0.1:8000

# Producción (Render):
# VITE_API_URL=https://podogest-backend.onrender.com
